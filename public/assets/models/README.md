# Optional glTF assets
Drop optimized `.glb` weapon or environment files here and add them to `/assets/manifest.json`. The game keeps procedural fallbacks so deployment never depends on external assets.
